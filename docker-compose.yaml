services:
  webhook-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gowa-webhook-api
    env_file:
      - .env
    expose:
      - "8000"
    restart: unless-stopped
    mem_limit: 16m
    memswap_limit: 16m
    cpus: "0.05"